apiVersion: v1
kind: PersistentVolume
metadata:
  name: upload-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/uploads"